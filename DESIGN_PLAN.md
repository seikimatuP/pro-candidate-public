# Design Implementation Plan: Dashboard + PlayerListPage

> **ステータス（2026-07-27追記）**: `DashboardRedesign.tsx`（`/dashboard`ルート）はオーナー確認の結果、
> 正規のダッシュボードではないと判断され削除済み。本計画（Variant Dベースの`Dashboard.tsx`書き換え）は
> 終了・棚上げ扱いとする。以下は当時の検討記録として保持する。

## Summary

- **Scope:** page
- **Target:** `src/pages/Dashboard.tsx`（トップページ）
- **Winner variant:** F（Variant D ベース + 円グラフ）
- **Key improvements:** ナラティブ型データ表示、高校/大学・都道府県別の円グラフ、最新届け出セクション削除

## Design Direction

### 採用したコンセプト: データストーリーテリング

- 数字を並べるだけでなく「今年の志望届はこうなっている」とストーリーで伝える
- 前年比の変化量を常に表示し、データに意味を持たせる
- Sparkline をStatCard内に埋め込み、トレンドを一目で把握可能に

### 具体的な変更点

1. **ナラティブ導入文**: 「今年の志望届提出は156名。前年から+23名の増加傾向。」
2. **NarrativeStatCards**: 高校生/大学生/学校数の3カード（sparkline + 前年比付き）
3. **円グラフ2つ追加**: 高校/大学比率のドーナツチャート + 都道府県別分布のドーナツチャート
4. **推移チャート**: バーチャート形式で週次推移を表示（差分ラベル付き）
5. **都道府県ランキング**: 横棒バー + 順位表示
6. **最新届け出セクション**: 削除（トップページには不要と判断）

## Files to Change

- [ ] `src/pages/Dashboard.tsx` - メインコンポーネント書き換え
- [ ] `src/styles/dashboard.css` - MUIベースに統一するため不要部分を削除
- [ ] `src/contexts/ThemeContext.tsx` - セマンティックカラーにチャート用色を追加（必要に応じて）

## Implementation Steps

### 1. Dashboard.tsxの書き換え

- 既存のStatCard群を NarrativeCard コンポーネントに置き換え
- ナラティブ導入文（年度 + 合計 + 前年比）を追加
- Sparkline SVGコンポーネントを作成（インライン or 共通化）
- ChangeIndicator コンポーネント（前年比表示）を作成

### 2. 円グラフセクション追加

- chart.js + react-chartjs-2（既存依存）を使用
- `ArcElement` を Chart.js に登録
- 高校/大学比率のドーナツチャート: `mockStats.highschoolPlayers` / `universityPlayers`
- 都道府県別分布のドーナツチャート: 上位5県 + 「その他」

### 3. 推移チャートの更新

- 既存の TrendChart を週次バーチャート形式に変更
- 各バーに差分ラベル（+25, +43 等）を表示
- 最新週をアクセントカラーでハイライト

### 4. 都道府県ランキング

- 横棒バー + 順位番号
- 1位をアクセントカラー、2位以下を薄いalphaで表示

### 5. 最新届け出セクション削除

- RecentPlayers コンポーネントの呼び出しを削除
- ダッシュボードから最新選手リストを除外

### 6. dashboard.css の整理

- MUI sx prop に移行済みのスタイルを削除
- 残すべきグローバルスタイル（あれば）のみ保持

## Component API

### NarrativeCard

```typescript
interface NarrativeCardProps {
  headline: string; // カードのラベル（例: "高校生の提出状況"）
  value: number | string; // メイン数値
  unit: string; // 単位（例: "名"、"校"）
  change?: number; // 前年比変化量
  changeLabel?: string; // 変化の説明（例: "前年比"）
  sparkData?: number[]; // sparklineデータ
  sparkColor?: string; // sparkline色
  accentColor: string; // ラベルのアクセント色
}
```

### Sparkline

```typescript
interface SparklineProps {
  data: number[];
  color: string;
  width?: number; // default: 80
  height?: number; // default: 24
}
```

### ChangeIndicator

```typescript
interface ChangeIndicatorProps {
  value: number;
  suffix?: string; // default: "名"
}
```

## Required UI States

- **Loading:** NarrativeCard にスケルトン表示、チャートにスピナー
- **Empty:** 「データがまだありません」メッセージ
- **Error:** エラーバナーで再試行ボタン

## Color Tokens

- **高校生**: `#00897b`（ライト） / `#4db6ac`（ダーク）
- **大学生**: `#ef6c00`（ライト） / `#ffb74d`（ダーク）
- **プライマリ**: theme.palette.primary.main
- **増加**: `#2e7d32`
- **減少**: `#c62828`
- **円グラフ都道府県**: primaryColor, highschoolColor, universityColor, blue, purple, green, red

## Accessibility Checklist

- [ ] ドーナツチャートにaria-labelとrole="img"を付与
- [ ] Sparkline SVGにaria-hidden="true"（装飾的）
- [ ] ChangeIndicator に sr-only テキスト（「前年比プラス23名」）
- [ ] 色だけに依存しない情報伝達（数値ラベル併記）
- [ ] WCAG AA コントラスト比確認
- [ ] prefers-reduced-motion 対応

## Testing Checklist

- [ ] NarrativeCard の単体テスト（正常値・変化なし・負値）
- [ ] 円グラフレンダリングテスト
- [ ] ダッシュボード統合テスト
- [ ] ダークモード表示確認
- [ ] モバイルレスポンシブ確認

---

_Generated by Design Lab_
