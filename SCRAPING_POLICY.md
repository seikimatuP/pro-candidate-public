# スクレイピングポリシー

最終更新日: 2026年8月22日

## 概要

本ドキュメントは、プロ野球志望届データ管理システムにおけるウェブスクレイピングの方針と遵守事項を定めます。

## 対象サイト

| サイト               | URL                                               | robots.txt        |
| -------------------- | ------------------------------------------------- | ----------------- |
| 日本高等学校野球連盟 | https://www.jhbf.or.jp/pro-aspiring               | 存在しない（404） |
| 全日本大学野球連盟   | https://www.jubf.net/system/prog/procandidate.php | 存在しない（404） |

## 遵守事項

### 1. アクセス頻度

- **リクエスト間隔**: 最低1秒（1000ms）
- **タイムアウト**: 30秒
- **リトライ回数**: 最大3回

### 2. User-Agent

```
Mozilla/5.0 (compatible; DataScraper/1.0; AWS Lambda; +mailto:yuta.nozue@gmail.com)
```

明確なUser-Agentを設定し、クローラーであることと連絡先を明示しています（2026-07-20、リーガルレビューの推奨により連絡先を追加）。

### 3. データ利用

- 収集データは統計・分析目的に限定
- 公開情報のみを対象
- 商用再配布は行わない

### 4. サーバー負荷

- 同時接続数の制限
- オフピーク時間帯での実行を推奨
- 定期実行は1日1回程度。実装は EventBridge の定期実行ルール（平日 JST 17:30、`ScrapingScheduleRule`）で、2026-08-19 に有効化した
- 定期実行のルールは **prod 環境にのみ作る**。dev にも同じルールがあると取得元の公式サイトへ dev/prod から二重にアクセスすることになるため（dev で挙動を試すときはテスト用ルール `TestScrapingRule` を手動で有効化する）
- 志望届の公示はシーズンが限られるため、通年では回さない。稼働期間（開始日〜終了日）を SSM パラメータ `/pro-candidate/{stage}/scraping-schedule-period` に JSON（例: `{"start":"2026-09-01","end":"2026-09-30"}`）で持ち、Lambda が起動時に「今日が期間内か」を JST の暦日で判定して、期間外は何もせず終了する。既定値は空（`{"start":"","end":""}`）で、この状態では実行しない。未設定・値の不正・取得失敗はすべて「期間外」として扱う
- 上記はコード変更・デプロイなしに `aws ssm put-parameter --overwrite` だけで変更できる（設定・確認・停止の運用手順は非公開の運用ドキュメントで管理）
- 手動実行（管理画面からの実行）は稼働期間の判定対象外。シーズン外の再取得を塞がないため

## 法的根拠

### 調査結果（2025年12月14日実施）

| 確認項目       | www.jhbf.or.jp    | www.jubf.net      |
| -------------- | ----------------- | ----------------- |
| robots.txt     | 404（存在しない） | 404（存在しない） |
| /privacy/      | 404（存在しない） | 404（存在しない） |
| /terms/        | N/A               | 404（存在しない） |
| 利用規約ページ | **確認できず**    | **確認できず**    |

### 法的見解

1. **robots.txt**: 両サイトともrobots.txtが存在せず、クローラーの制限は確認されていません

2. **利用規約**: 公開された利用規約ページが確認できませんでした。これは以下を意味する可能性があります：
   - 明示的な禁止事項がない
   - または内部規定として非公開

3. **公開情報**: スクレイピング対象は、各連盟が公式に公開しているプロ志望届データです

4. **著作権**: データの形式変換・集計のみを行い、元データの複製配布は行いません

> ⚠️ **注意**: 利用規約が見つからないことは「許可されている」ことを意味しません。
> 不安がある場合は、各連盟に直接お問い合わせください。
>
> - 日本高野連: https://www.jhbf.or.jp/
> - 全日本大学野球連盟: https://www.jubf.net/contact

## 問題発生時の対応

### 連盟（対象サイト）からの停止要請

対象サイトからスクレイピング停止の要請があった場合：

1. 即座にスクレイピングを停止
2. 該当データの削除を検討
3. 代替手段（API提供など）の交渉

### 削除請求への対応（選手本人・法定代理人から）

選手本人（未成年者の場合は保護者等の法定代理人を含む）から自己のデータについて削除・利用停止等の請求があった場合、以下のフローで対応する：

1. **受付**: PRIVACY_POLICY.md 記載のお問い合わせ窓口で受け付ける
2. **本人確認**: 請求者が本人または法定代理人であることを合理的な方法で確認する（氏名・所属校・届出年度の照合等。過剰な本人確認書類は求めない）
3. **対応**: 受付から原則2週間以内に、該当データの削除または利用停止を実施する。対応できない正当な理由がある場合はその理由を回答する
4. **記録**: 受付日・請求内容・対応内容・完了日を記録として残す
5. **再収集の防止**: 削除したデータが定期スクレイピングで再取得されないよう、除外リストで管理する

#### 除外リストの運用手順

除外リストの実体は、データバケット（`S3_DATA_BUCKET`）上の `config/exclusion-list.json`。
氏名と学校名が一致する選手を、次の2つの経路で取り除く。

1. **スクレイピングLambda**: 毎回このファイルを読み、S3へ保存する前に取り除く（再収集の防止）
2. **API Lambda**: S3から読んだデータを配信する前に取り除く（保存済みの過年度データにも効かせるため）

ファイルが無い場合は「除外なし」として通常どおり動作する。
一方、ファイルを読めなかった場合（権限エラー・JSON不正など）は処理を止める。
スクレイピングは保存せずに失敗として終わり、APIは 503 を返す。
除外を確認できないまま動くと、削除請求済みの選手を保存・配信してしまうため。

削除請求を受け付けたら、上記フローの手順3（対応）で既存データを削除するのに続けて、
このJSONへ1件追記する。過年度の掲載分は、追記した時点でAPI側の除外が効くため
年度ごとにファイルを直す必要はない。

1. 現在の内容を取得する（初回は `{ "players": [] }` から作る）

   ```bash
   aws s3 cp "s3://${S3_DATA_BUCKET}/config/exclusion-list.json" ./exclusion-list.json
   ```

2. `players` 配列へ請求者を追記する。`name` と `school` はスクレイピング元サイトの表記に合わせる
   （空白や英字の大小の違い、氏名末尾のふりがな注記「氏名(ふりがな)」の有無は照合時に吸収される）

   ```json
   {
     "players": [
       {
         "name": "山田太郎",
         "school": "○○高等学校",
         "reason": "deletion-request",
         "addedAt": "2026-08-18T00:00:00.000Z"
       }
     ]
   }
   ```

3. アップロードする

   ```bash
   aws s3 cp ./exclusion-list.json "s3://${S3_DATA_BUCKET}/config/exclusion-list.json"
   ```

4. 手元にダウンロードしたファイルは作業後に削除する。除外リスト自体が個人情報なので、
   リポジトリにはコミットしない（Lambdaのログにも件数だけが出力され、氏名・学校名は残らない）

5. 追記後すぐに、管理者でログインした選手一覧で対象選手が表示されなくなったことを確認する
   （氏名を含む一覧は admin 限定のため、未ログインの画面では確認できない。
   API側の除外リストのキャッシュは最大5分で、反映までに数分かかることがある）

6. 次回スクレイピング後、対象選手が再登録されていないことを確認する

受付、本人・法定代理人確認、暫定非表示、完了確認、記録の最小化は
`docs/PRIVACY_REQUEST_RUNBOOK.md` に従う。

### 定期再確認

- 対象サイトの robots.txt・利用規約の有無を四半期に1回再確認し、本ドキュメントの調査結果表を更新する（直近確認: 2026-07-20、両サイトとも変化なし）
- 規約の新設・変更でスクレイピングが制限された場合は、即座に停止して対応を検討する

## 連絡先

個人情報に関する請求・問い合わせは PRIVACY_POLICY.md 記載の窓口へ。技術的な問い合わせやスクレイピングに関する質問は、GitHubリポジトリのIssueからご連絡ください。

## 変更履歴

| 日付       | 変更内容                                                                                                                                              |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2025-12-14 | 初版作成。robots.txt確認結果を記載                                                                                                                    |
| 2026-07-20 | リーガルレビューを受け、個人からの削除請求フロー・定期再確認を追加。robots.txt/規約の現況を再確認（変化なし）                                         |
| 2026-07-20 | 公開プライバシーページ（/privacy.html）の設置と、User-Agentへの連絡先追加（個人情報保護法21条対応）                                                   |
| 2026-08-18 | 再収集の防止（除外リスト）をスクレイピングLambdaに実装し、運用手順を追記                                                                              |
| 2026-08-19 | 除外リストの照合をふりがな付き氏名に対応。API配信時にも除外を適用（過年度データ対応）。読み込み失敗時は処理を止める挙動に変更。定期実行ルールを有効化 |
| 2026-08-19 | 定期実行をprod環境のみに限定し、稼働期間（SSMパラメータで指定・既定は実行しない）による実行制御を追加                                                 |
| 2026-08-22 | HTTP タイムアウト、上限付き再試行、Retry-After 尊重、要求開始間隔の直列制御を実処理へ追加。氏名データを admin 限定へ変更                              |
